# 纯前端模式配置说明
#
# 本项目为纯前端应用，无需后端服务。
# 所有功能均通过前端直连第三方 API 实现：
#   - 股票数据：直连东方财富 API（支持 CORS）
#   - 天气数据：直连天气 API（支持 CORS）
#
# 推送代码到 GitHub main 分支后自动构建部署到 GitHub Pages，推送即用。
# 无需配置 VITE_API_BASE，该变量已不再使用。
