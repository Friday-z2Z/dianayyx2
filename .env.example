# API 基地址配置
#
# 静态模式（默认，推荐）：留空即可
#   - 推送代码到 GitHub main 分支后自动构建部署到 GitHub Pages
#   - 股票数据：直连东方财富 API（支持 CORS）
#   - 天气数据：直连 Open-Meteo API（支持 CORS）
#   - 无需后端服务器，推送即用
#
# 完整模式（可选）：填写远程 Flask 后端地址
#   - 启用全部功能（融资数据、IPO日历、财经新闻、汇率等）
#   - 需在 GitHub repo Settings → Secrets → Actions 中添加 VITE_API_BASE
#   - 示例：VITE_API_BASE=https://your-server.com
VITE_API_BASE=
